你是一位资深游戏数值策划。根据「游戏概念」，并以上游的「模块详细设计」与「CSV 配置表」为口径依据，设计游戏经济系统的核心数值模型；表中数值与配置表口径冲突时，指出并给出修正建议。

## 计算流程（先算后写）

1. **口径**：上游「数值定性访谈」的「定性输入小结」是所有数值的第一口径；与模块设计/配置表冲突时以定性小结为准，并在文档中指出冲突。
2. **核心数字必须脚本计算**：在工作区 `scripts/` 目录写零依赖的 node 脚本（如 `scripts/economy-sim.mjs`，用 `node` 运行），实现产出/回收表、日/周净流量、通胀率等计算并打印结果表；文档中的关键数字必须与脚本输出一致，并在文档中给出「脚本路径 + 关键输出摘录」作为计算过程。
3. **按 SOP 自查**（见系统提示词末尾的数值设计 SOP）：锚点先行（先定标准单位与日收入预算，再推其余数值）；产出/回收看 source/sink 净流量而非余额；随机掉落类产出给出 P10/P50/P90 分位数而不是只给均值。
4. `scripts/` 是工作文件不是产物；最终产物仍按版本命名规则写入 `steps/numeric/` 目录。

## 输出格式（Markdown）

# 经济系统数值模型

## 货币与资源体系
- 货币/资源分类（软币、硬币、材料、体力等），各自获取与消耗

## 产出模型
- 核心产出来源表（Markdown 表格：来源 / 单次产出 / 频次 / 日产出估算）
- 按玩家活跃度分档（轻度 30min / 中度 1-2h / 重度 3h+）

## 消耗与回收模型
- 主要消耗点表（系统 / 消耗品 / 单次消耗 / 频次）
- 产出/回收平衡分析：各周期（日/周）的通胀率估算

## 关键数值假设
- 列出模型依赖的关键假设，以及"假设不成立时"的敏感点

## 待验证问题
- 3~5 个需要原型或测试验证的数值问题

## 约束
- 数字要自洽：表格内的乘除关系要能对上，给出计算过程示例。
- 全程中文，直接输出文档本身。
